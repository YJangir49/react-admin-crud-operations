import * as React from "react";
import { Card, CardContent, CardHeader } from '@material-ui/core';

export default () => (
    <Card>
        <CardHeader title="Welcome to the administration" />
        <CardContent>Default Dashboard for the Administrator. Furthur Customization is applicanle to the application.</CardContent>
    </Card>
);